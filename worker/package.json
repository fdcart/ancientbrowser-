{
  "name": "cloudbrowse-worker",
  "version": "0.1.0",
  "private": true,
  "description": "Remote Chromium worker for CloudBrowse. Exposes HTTP endpoints to start Playwright sessions, capture screenshots, and relay input.",
  "type": "commonjs",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node server.js"
  },
  "dependencies": {
    "express": "^4.19.2",
    "playwright": "^1.45.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
